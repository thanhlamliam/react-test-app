declare module "d3-hierarchy" {
  import {
    stratify,
    cluster,
    hierarchy,
    Node,
    pack,
    packSiblings,
    packEnclose,
    partition,
    tree,
    treemap,
    treemapBinary,
    treemapDice,
    treemapSlice,
    treemapSliceDice,
    treemapSquarify,
    treemapResquarify,
  } from "d3-hierarchy";

  export {
    stratify,
    cluster,
    hierarchy,
    Node,
    pack,
    packSiblings,
    packEnclose,
    partition,
    tree,
    treemap,
    treemapBinary,
    treemapDice,
    treemapSlice,
    treemapSliceDice,
    treemapSquarify,
    treemapResquarify,
  };
}